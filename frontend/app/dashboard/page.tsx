export default function DashboardPage() {
  return (
    <div>
      <h1>Your reviews</h1>
      {/* TODO: List past reviews and wire up the UploadForm component. */}
      <div>
        {/* TODO: Upload area — drop zone / file picker for contract PDFs. */}
        Upload area
      </div>
    </div>
  );
}
