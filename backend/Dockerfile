# TODO: Production Dockerfile for the FastAPI backend
# - Base image (e.g. python:3.12-slim)
# - Install dependencies from requirements.txt
# - Copy the app package
# - Expose the API port
# - Run uvicorn app.main:app
