from fastapi import APIRouter

router = APIRouter()

# TODO: User endpoints
# - get current user — return the authenticated Clerk user profile
# - get usage — return review count / free-tier remaining
