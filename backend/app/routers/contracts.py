from fastapi import APIRouter

router = APIRouter()

# TODO: Contract endpoints
# - POST upload — accept a contract PDF and start a review
# - GET review — return a review report by id
# - GET history — return the current user's past reviews
