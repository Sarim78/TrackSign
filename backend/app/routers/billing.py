from fastapi import APIRouter

router = APIRouter()

# TODO: Billing endpoints
# - create checkout session — start a Stripe Checkout for the paid plan
# - stripe webhook — handle Stripe events (subscription created/updated/canceled)
