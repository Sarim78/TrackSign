def verify_clerk_token(token: str):
    """Verify a Clerk session token and return the authenticated identity."""
    # TODO: Verify the Clerk JWT (signature, expiry, issuer) and return the user id / claims.
    ...
